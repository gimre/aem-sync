
'use strict'

/*
 * electrolyte is retarded and reactivex is a hipster
 */

const { Observable } = require( '@reactivex/rxjs' )

exports = module.exports = ( ) => Observable

exports[ '@singleton' ] = true